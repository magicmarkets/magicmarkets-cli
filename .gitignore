# Secrets — never commit a real API key
.env
.env.local
*.pem

# Build output
/build/
/dist/
# Anchored to the repo root so these cannot swallow internal/magicmarkets/.
# `go build ./...` names the binary after the module directory.
/magicmarkets
/magicmarkets-cli

# Go
*.test
*.out
coverage.*

# Editors and OS
.DS_Store
.idea/
.vscode/
*.swp
